name: 同步 Scoop CTF 仓库

on:
  schedule:
    # 每天北京时间早上8点运行 (UTC时间0点)
    - cron: '0 0 * * *'
  push:
    # 当工作流文件本身被修改时触发
    paths:
      - '.github/workflows/sync-buckets.yml'
  workflow_dispatch:
    # 允许手动触发
    inputs:
      bucket:
        description: '指定要同步的仓库（留空表示同步所有）'
        required: false
        default: ''

env:
  TZ: Asia/Shanghai

jobs:
  sync-buckets:
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      max-parallel: 1  # 一次只运行一个job，避免并发推送冲突
      matrix:
        bucket:
          - name: sec
            repo: tldro/scoop-security
          - name: ar
            repo: arch3rPro/PST-Bucket
          - name: ctftools
            repo: kengwang/scoop-ctftools-bucket
    
    steps:
    - name: 检出代码库
      if: github.event.inputs.bucket == '' || github.event.inputs.bucket == matrix.bucket.name
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_TOKEN }}
        fetch-depth: 0

    - name: 设置 Git
      if: github.event.inputs.bucket == '' || github.event.inputs.bucket == matrix.bucket.name
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"

    - name: 同步仓库内容
      if: github.event.inputs.bucket == '' || github.event.inputs.bucket == matrix.bucket.name
      run: |
        echo "正在同步 ${{ matrix.bucket.name }} (repo: ${{ matrix.bucket.repo }})..."
        
        # 创建目标目录
        TARGET_DIR="buckets"
        SCRIPTS_DIR="scripts"
        mkdir -p "$TARGET_DIR"
        mkdir -p "$SCRIPTS_DIR"
        
        # 创建临时目录进行克隆
        TEMP_DIR="temp_${{ matrix.bucket.name }}"
        git clone --depth 1 https://github.com/${{ matrix.bucket.repo }}.git "$TEMP_DIR"
        
        # 复制 bucket 目录下的 JSON 文件 (不覆盖已存在的)
        if [ -d "$TEMP_DIR/bucket" ]; then
          echo "复制新的 JSON 文件..."
          cp -n "$TEMP_DIR/bucket/"*.json "$TARGET_DIR/" 2>/dev/null || true
        fi
        
        # 复制 scripts 目录下的文件
        if [ -d "$TEMP_DIR/scripts" ]; then
          cp -r "$TEMP_DIR/scripts/"* "$SCRIPTS_DIR/" 2>/dev/null || true
        fi
        
        # 清理
        rm -rf "$TEMP_DIR"

    - name: 提交并推送更改
      if: github.event.inputs.bucket == '' || github.event.inputs.bucket == matrix.bucket.name
      run: |
        git add buckets/ scripts/
        if git diff --cached --quiet; then
          echo "没有检测到更改"
        else
          git commit -m "chore: 同步 ${{ matrix.bucket.name }} (${{ matrix.bucket.repo }}) - $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 推送重试逻辑
          for i in {1..5}; do
            git pull --rebase origin main
            if git push origin main; then
              echo "成功推送更改"
              exit 0
            fi
            echo "推送失败，等待重试 ($i/5)..."
            sleep $((i * 2))
          done
          exit 1
        fi

  update-readme:
    needs: sync-buckets
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码库
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_TOKEN }}
        fetch-depth: 0
        
    - name: 更新 README 统计信息
      run: |
        current_time=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')
        
        # 统计数据
        TOTAL_APPS=$(find buckets -name "*.json" | wc -l)
        
        # 构建 Header
        README_BODY="# 🛡️ scoop-ctf\n\n"
        README_BODY="${README_BODY}[![GitHub Stars](https://img.shields.io/github/stars/scoop-ctf/scoop-ctf?style=flat-square)](https://github.com/scoop-ctf/scoop-ctf/stargazers) "
        README_BODY="${README_BODY}[![GitHub License](https://img.shields.io/github/license/scoop-ctf/scoop-ctf?style=flat-square)](LICENSE) "
        README_BODY="${README_BODY}[![Auto Sync](https://img.shields.io/badge/Auto%20Sync-Enabled-blue?style=flat-square&logo=github-actions)](.github/workflows/sync-buckets.yml)\n\n"
        
        README_BODY="${README_BODY}这是一个专门为安全研究人员和 CTF 玩家设计的 Scoop bucket 集合，每日从多个顶级安全仓库自动同步更新。\n\n"
        
        # 安装指南
        README_BODY="${README_BODY}## 🚀 快速开始\n\n\`\`\`bash\n# 添加 bucket\nscoop bucket add scoop-ctf https://github.com/scoop-ctf/scoop-ctf\n\n# 搜索工具 (例如 fscan)\nscoop search fscan\n\n# 安装工具\nscoop install scoop-ctf/fscan\n\`\`\`\n\n"
        
        # 统计
        README_BODY="${README_BODY}## 📊 统计信息\n\n"
        README_BODY="${README_BODY}目前仓库共包含 **$TOTAL_APPS** 个安全工具清单。\n\n"
        
        # 来源仓库列表
        README_BODY="${README_BODY}### 来源仓库\n\n"
        README_BODY="${README_BODY}| 仓库名称 | 来源地址 |\n"
        README_BODY="${README_BODY}| :--- | :--- |\n"
        README_BODY="${README_BODY}| **sec** | [tldro/scoop-security](https://github.com/tldro/scoop-security) |\n"
        README_BODY="${README_BODY}| **ar** | [arch3rPro/PST-Bucket](https://github.com/arch3rPro/PST-Bucket) |\n"
        README_BODY="${README_BODY}| **ctftools** | [kengwang/scoop-ctftools-bucket](https://github.com/kengwang/scoop-ctftools-bucket) |\n"
        README_BODY="${README_BODY}| **main** | 🛡️ 本地维护工具 |\n\n"
        
        # 贡献指南
        README_BODY="${README_BODY}## 🛠️ 手动添加软件\n\n如果你想手动添加新的工具清单，请直接将 \`.json\` 文件放入 \`buckets/\` 目录并提交 PR。\n\n"
        
        README_BODY="${README_BODY}> [!NOTE]\n"
        README_BODY="${README_BODY}> 数据最后更新于: \`$current_time\`\n"
        
        # 写入文件
        echo -e "$README_BODY" > README.md
        
    - name: 提交并推送 README
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"
        git add README.md
        if ! git diff --cached --quiet; then
          git commit -m "docs: 更新统计信息 ($(date '+%Y-%m-%d %H:%M:%S'))"
          
          # 推送重试逻辑
          for i in {1..5}; do
            git pull --rebase origin main
            if git push origin main; then
              echo "成功推送 README"
              exit 0
            fi
            echo "推送失败，等待重试 ($i/5)..."
            sleep $((i * 2))
          done
          exit 1
        fi
